# 視界助行 AI：行動介面設計

## 設計目標

本應用採取**語音優先、低視覺負荷、單手可操作**的直向 9:16 介面。其核心目的不是取代白手杖、導盲犬、定向行動訓練或道路安全判斷，而是在使用者主動啟動下，以清楚、短句、可重複的語音，補充周遭環境、文字、位置與時間資訊。所有高風險提示都應用「可能」或「請確認」描述不確定性，避免將影像辨識結果宣稱為安全保證。

介面遵循 Apple Human Interface Guidelines 的可預測層級與足夠的觸控目標，同時以 Android TalkBack 的閱讀順序為主要驗收標準。每個可操作元件均應提供明確的 `accessibilityLabel`、角色與狀態；所有語音內容必須能用大按鈕重新報讀。

## 螢幕清單與功能

| 螢幕 | 主要內容 | 使用者可完成的操作 |
| --- | --- | --- |
| 語音優先首頁 | 目前模式、網路狀態、最近一次提示與四個主要功能入口 | 說出或點選「辨識環境」、「建立記事」、「現在時間」、「開啟瀏覽器」 |
| 環境辨識 | 全螢幕相機預覽、辨識狀態、最新語音摘要、停止按鈕 | 拍攝或擷取目前畫面、以 AI 描述重要物件與可見文字、重播結果 |
| 位置與時間 | 精度狀態、座標/地址摘要、目前日期時間、報讀按鈕 | 報讀目前時間、要求定位、複誦位置資訊 |
| 語音記事本 | 記事清單、語音錄製/文字輸入、最近的轉錄文字與報讀控制 | 說出一段記事、儲存文字與語音附件、重新報讀或刪除記事 |
| 語音瀏覽器 | 語音指令輸入、網址/搜尋字串、開啟系統瀏覽器的確認狀態 | 說出「搜尋…」或「開啟…」，以系統瀏覽器開啟安全編碼後的網址 |
| 設定與安全說明 | 語音速度、回覆詳略、震動開關、隱私處理說明與功能限制 | 調整報讀偏好、檢視權限用途、理解影像結果僅作輔助 |

## 主要使用流程

| 情境 | 流程 |
| --- | --- |
| 即時環境輔助 | 使用者在首頁說出「辨識環境」或點擊主按鈕 → 授權相機 → 相機畫面先以語音提示「鏡頭已開啟」→ 使用者按住或點擊「描述現在畫面」→ 系統拍攝單張畫面並辨識 → 以「前方偵測到…，請自行確認」形式報讀 → 使用者可說「再說一次」或「停止」。 |
| 查詢位置與時間 | 使用者說「現在時間」或「我在哪裡」→ 系統取得裝置時間或請求位置權限 → 先報讀最可靠資訊，再補充定位精度與可用地址 → 若資訊不可用，清楚告知原因與可採取動作。 |
| 建立語音記事 | 使用者說「建立記事」→ 按下大面積錄音按鈕 → 停止後將音訊轉成文字 → 顯示並報讀摘要 → 使用者說「儲存」或點擊儲存 → 記事及可選音訊附件儲存於裝置。 |
| 語音開啟網頁 | 使用者說「搜尋公車資訊」或「開啟政府網站」→ 系統轉錄指令 → 顯示、報讀將要開啟的搜尋/網址 → 使用者確認 → 用系統瀏覽器開啟，避免在應用內自行模擬瀏覽器控制。 |

## 互動與無障礙細節

應用首頁以單欄式大型控制項呈現，首要的「辨識環境」按鈕置於拇指可及的中下區域。所有按鈕至少採用 56dp 的觸控區，文字採 20sp 以上；避免僅以顏色傳遞訊息。錄音、相機與辨識等持續動作必須提供顯著且固定位置的「停止」控制，避免使用者被困在全螢幕流程中。

語音回覆依照「先警示、後描述、再提供下一步」排序。例如：「請注意，畫面中可能有階梯。前方下方可見三階。請以白手杖或其他方式確認。」語音不得在未經觸發時持續播放；若辨識過程逾時，應提供震動與短語音回饋。影像、音訊與位置資料預設僅為完成該次功能而處理；本機記事不需登入帳號。

## 色彩與視覺語言

| 用途 | 色彩 | 理由 |
| --- | --- | --- |
| 主色 | 深靛藍 `#153D73` | 對淺色背景有穩定對比，傳達可靠與專注。 |
| 輔助亮色 | 明亮青綠 `#00A6A6` | 用於可操作與已啟用狀態，兼具可辨識性。 |
| 安全提示 | 琥珀 `#A95700` | 用於需要留意而非確定危險的提示，避免過度恐嚇。 |
| 錯誤/停止 | 深紅 `#A61B1B` | 用於停止、權限失敗與操作錯誤，文字對比需符合可讀性。 |
| 背景/文字 | 近白 `#F8FAFC`、墨藍 `#0D1B2A` | 提供高明度差，並支援深色模式對應。 |

## 資料模型

應用以裝置本機儲存為預設。`Note` 包含 `id`、`text`、`createdAt`、`audioUri?` 與 `locationLabel?`。`RecognitionResult` 包含 `summary`、`caution?`、`detectedAt`、`location?` 與 `source`。`AccessibilityPreferences` 包含 `speechRate`、`detailLevel`、`hapticsEnabled` 與 `autoReadResults`。影像與錄音會在使用者主動擷取時才送交分析，並在畫面明確顯示處理狀態。

## 技術與功能邊界

首版將以**單張相機擷取後的 AI 分析**提供近即時環境描述；它不是具備深度感測器的 3D 導航或避障系統。位置採取裝置 GPS/網路定位，只有在系統提供時才顯示地址文字。語音記事可保留錄音及其轉錄；文字報讀採裝置內建 TTS。語音瀏覽功能以理解指令並交給 Android 系統瀏覽器開啟為界，不會自動操作第三方網站帳號、填寫表單或繞過網站的登入/安全機制。
